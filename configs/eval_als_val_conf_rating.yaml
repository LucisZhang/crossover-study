# ALS confidence sweep (Phase 3, E10): weighting=rating (c=1+alpha*r, r in 1..5)
kind: eval
model:
  name: als
  params:
    rank: 128
    reg_param: 0.01
    alpha: 10.0
    max_iter: 25
    weighting: rating
    factors_root: data/eval/als
protocol:
  eval_split: val
  knowledge_cutoff: train_end
  k_list: [10, 20, 50]
  batch_size: 1024
bootstrap:
  n_resamples: 1000
  seed: 20260805
seeds:
  model: 20260805
train:
  checkpoint_interval: 1
tables:
  five_core: local.gold.interactions_5core
  user_stats: local.gold.user_stats
  item_features: local.gold.item_features
  popularity: local.gold.popularity
cache_dir: data/eval/cache
