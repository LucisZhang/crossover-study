data/*
!data/MANIFEST.md
!data/MANIFEST_ML32M.md
.remote/
.venv/
__pycache__/
*.pyc
.pytest_cache/
.DS_Store
derby.log
metastore_db/
spark-warehouse/
logs/

# Agent worktrees (session infrastructure, never repo content)
.claude/worktrees/

# Semantic-search exhibit assets (Phase 6 T35, UPGRADE_PLAN §12 cut order #2):
# 79.5MB of int8 payload + vendored MiniLM/ONNX runtime, regenerated by
# `make demo-assets`. Never committed; the SHA-256s that make the download
# verifiable live in demo/README.md, which IS committed.
demo/data/search/
demo/vendor/
